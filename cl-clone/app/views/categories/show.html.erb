<%= @category.name %><br>

<% @articles.each do |article| %>
      <%= link_to(article.title, category_article_path(article)) %>
      <%= link_to("Edit", edit_category_article_path(article)) %>
      <%= link_to("Delete", category_article_path(article), method: :delete) %>
      <br>
<% end %>

<%= link_to 'Create article', new_category_article_path(@category) %>

